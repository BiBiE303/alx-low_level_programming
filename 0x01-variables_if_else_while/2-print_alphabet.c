#include <stdio.h>
/**
 * main - prints alpha in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lc;

v	for (lc = 'a'; lc <= 'z'; lc++);

	{
		putchar(lc);
	}
	putcar('\n');
	return (0);
}
